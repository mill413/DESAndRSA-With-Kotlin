import bits.Bits

fun main() {
    val a = Bits(
        arrayListOf(
            1, 1, 1, 0, 0, 1, 0
        )
    )
    val b = Bits(
        arrayListOf(
            1, 0, 1, 1, 0
        )
    )

//    println(a xor b)
}